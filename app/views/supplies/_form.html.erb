<%= form_for(@supply) do |f| %>
  <% if @supply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supply.errors.count, "error") %> prohibited this supply from being saved:</h2>

      <ul>
      <% @supply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :supermarket_id %><br />
    <%= f.number_field :supermarket_id %>
  </div>
  <div class="field">
    <%= f.label :origin_id %><br />
    <%= f.number_field :origin_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
